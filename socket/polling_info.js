var _ = require('../lib/underscore');
var loggerModule = require('../utils/logger');
var logger = loggerModule(loggerModule.level.LOG);

function polling_info() {
  
}

// Export this file as a module
module.exports = polling_info;